---
description: Run terraform init + plan against an example under examples/azure/<module>/<variant>/. Auto-uses local Azure CLI credentials when available.
argument-hint: <module-name> [complete|minimum]
---

Invoke the `tf-test` skill with argument: $ARGUMENTS

Follow `.github/skills/tf-test/SKILL.md` exactly.
