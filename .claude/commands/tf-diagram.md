---
description: Generate a Draw.io architecture diagram from a config.yaml using vendor icon libraries (Azure/AWS/GCP) via the drawio MCP server.
argument-hint: <path to config.yaml | description>
---

Invoke the `tf-diagram` skill with argument: $ARGUMENTS

Follow `.github/skills/tf-diagram/SKILL.md` exactly. Prerequisite: a `.drawio` file must be open in the editor (drawio MCP server talks to the active editor). Apply `.github/instructions/drawio-azure-icons.instructions.md` preferences when placing shapes.
