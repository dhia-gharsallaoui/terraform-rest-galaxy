---
description: Refresh an existing Draw.io diagram after config.yaml changes — diff config vs diagram, then add/remove/edit cells.
argument-hint: [description of what changed]
---

Read and follow `.github/prompts/tf-diagram-update.prompt.md` exactly. Treat the argument below as `$input`.

$input = $ARGUMENTS
